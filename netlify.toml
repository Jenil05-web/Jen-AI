[build]
  base = "."
  command = "cd Fronetnd && npm install && npm run build"
  publish = "Fronetnd/dist"

[build.environment]
  VITE_API_BASE_URL = "https://jen-ai-backend.onrender.com/api"
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
