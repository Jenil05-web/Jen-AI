{
  "type": "module",
  "name": "jenai",
  "version": "1.0.0",
  "scripts": {
    "build": "cd Fronetnd && npm install && npm run build",
    "postbuild": "echo 'Build completed successfully'",
    "install:all": "npm install && cd Fronetnd && npm install && cd .."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.0",
    "nodemon": "^3.1.10",
    "openai": "^6.1.0"
  }
}
